FROM node:20-alpine

WORKDIR /app

# Copia manifests para cache de dependências
COPY package*.json ./

RUN npm install

# Copia o código-fonte
COPY . .

EXPOSE 5173

# Roda o Vite com --host para permitir conexões externas ao container
CMD ["npm", "run", "dev", "--", "--host"]
